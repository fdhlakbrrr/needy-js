import { needBox, needColumnSection, needText } from "needy";

export default function needHome(): Node {
  // const test = await fetch("https://jsonplaceholder.typicode.com/posts");
  // const data = await test.json();

  return needBox(null);
}
